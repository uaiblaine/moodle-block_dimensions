# Field map — `BLK` / `FLT` / `GST` / `RSP` · Block shell, filter bar, ghost card, responsive

> **Line citations, 2026-09-23.** Every `file:line` reference below — `styles.css`,
> `summary.mustache`, the three AMD modules and the PHP classes — was re-derived against the working
> tree on 2026-09-23. Where a section rather than one rule is the honest anchor, the citation is a
> banner name instead of a line range. Nothing pins these numbers: an edit to a cited file can move
> them.


The block shell is the only server-rendered part: `block_dimensions::get_content()` builds the
`summary` renderable, `summary::export_for_template()` exports **labels and flags only**
(`labelsjson`, `filtersettingsjson`, `showheading` / `showsearch` / `showsectionheaders`,
`favouritesenabled`, `isbs4`), and `summary.mustache` emits an empty skeleton. Everything inside it
is rendered client-side by `amd/src/filters.js` from calls to `block_dimensions_get_block_dataset`
— one on load, then one per card group a favourites-only first load left short and one per status
bucket on its first visit; `amd/src/state.js` holds the pure filter/search state and
`amd/src/filter_tabs_nav.js` turns each pill group into a scrollable platter. The filter bar is
**not** a template: it is string-concatenated in `renderFilterControls()` (`filters.js:181-296`),
the only definition of its markup, whose docblock says why every pill set is a radiogroup rather
than a tablist. Visual replica: the `screens/` files that coin the `BLK-*` (shell + states), `FLT-*`
(filter bar), `GST-*` (ghost card) and `RSP-*` (responsive) badges.

| ID | Label | Type | Origin | Data | Rule |
|---|---|---|---|---|---|
| `BLK-HEADING` | My competencies | h2 | `summary.mustache:41-45` + `styles.css:187-192` | `{{#str}}mycompetencies{{/str}}`, id `block-dimensions-heading-{containerid}` | gated on `showheading` (`dataset_provider::get_ui_config():692-694` — "never set" counts as **on**, unlike every other flag); the outer region's `aria-label` uses the same string, so hiding the heading never removes the landmark name |
| `BLK-SEARCH` | Search cards / Search… | text input | `summary.mustache:46-61` + `styles.css:225-252` | `searchlabel` (visually-hidden `<label for>`), `search_placeholder` | whole `.dims-search-row` gated on `showsearch` (`enable_search`, default **off**); typing debounces 120 ms (`filters.js:1273-1318`), normalises accents (`state.js:34-37`), and **force-clears** the favourite filter and all four tag filters (`filters.js:1284-1297`), so a search spans the whole plan bucket on screen and every competency card; whatever a favourites-only first load left out of those is fetched first (`filters.js:1299-1314`). A term already in the input when a load starts — Retry, or a value the browser put back, which fires no `input` event — is read by `loadData` (`filters.js:1573-1577`), whose search branch keeps the favourites filter off and fetches the same missing cards (`filters.js:1633-1641`), so the search never runs over favourites alone |
| `BLK-SEARCH-ICON` | — | magnifier icon | `summary.mustache:52` + `styles.css:216-223` | `fa fa-search`, `aria-hidden` | decorative only; absolutely positioned inside the wrapper with `pointer-events: none` so it never eats a click meant for the input |
| `BLK-SEARCH-CLEAR` | Clear (`core`) | icon button | `summary.mustache:58-60` + `styles.css:254-285` | `fa fa-times` | ships `style="display:none"`; switched to `flex` the moment the input is non-empty (`filters.js:1275-1277`), and once when the events are bound, for a value the browser put back (`filters.js:1268-1271`); back to `none` on click, which also returns focus to the input (`filters.js:1321-1332`) |
| `BLK-FILTER-TOGGLE` | Filters | icon button | `summary.mustache:62-67` + `styles.css:1605-1607` | `togglefilters`, `fa fa-sliders`, `aria-expanded` | `display:none` above 576 px — it exists only to toggle `.dims-filters-open` (`filters.js:1484-1487`); a `matchMedia` listener resets both the class and `aria-expanded` when the viewport grows back (`filters.js:1489-1496`), otherwise a panel opened on mobile stays "expanded" to AT on desktop. Because the control exists **only** below the breakpoint, all of its styling — rest, hover/`[aria-expanded="true"]`, and the focus ring — lives inside that same media query (`styles.css:1609-1637`), each declaration a `--block-dimensions-*` token rather than a light/dark pair: there is no separate dark rule left to keep in sync |
| `BLK-LOADING` | Loading (`core`) | status text | `summary.mustache:72-74` | `role=status`, `aria-live=polite` | visible on first paint and the only content until the web service answers. From then on `syncLoadingLine` (`filters.js:555-591`) derives it from what is still on its way, so no loader can switch it off under another: `state.groupLoads` counts the group loads in flight — the first request and every phase-2 fetch, counted in and out by `startGroupLoad` / `endGroupLoad` (`filters.js:593-617`) — and `state.statusLoading` names a bucket being fetched for the first time. While any group load is pending the line shows its own `loading` text; a bucket loading alone words it `statusloadingreview` / `statusloadingcomplete`; with neither it is hidden. `renderStatusSkeleton` and `clearStatusSkeleton` only re-derive it (`filters.js:985`, `:1005`). The text is rewritten only when it changes, because rewriting a polite live region announces it again, and the first request counts out after it has started any fetch of the rest, so the line does not blink between the two (`filters.js:1678-1679`) |
| `BLK-ERROR` | — | `alert alert-warning` | `summary.mustache:76-81` | `loaderror` | any rejected fetch — the first load, a phase-2 load or a status bucket — or a failed render; `showError` (`filters.js:626-646`) deliberately blanks the text, un-hides the box, then writes the message on the **next frame** — writing into a `display:none` `role="alert"` is not announced by several screen readers (WCAG 4.1.3) |
| `BLK-ERROR-RETRY` | Retry | button | `summary.mustache:78-80` + `styles.css:2186-2190` | `retry` | re-runs `loadData` (`filters.js:1469-1479`), which starts from scratch (`filters.js:1568-1583`): `resetSession()` (`state.js:108-132`) puts back every card list, kept bucket, completion flag, filter and the group-load count, and bumps `state.session`, so a response still on its way from before the retry is dropped when it lands; `resetView()` (`filters.js:1532-1554`) empties the filter hosts and both lists. So it starts a new session at phase 1 and on the bucket the server opens on, not at the failed phase, and it reads any search term back from the input. When the button holds focus as the retry starts, `focusBlock()` (`filters.js:1500-1515`) first moves focus to the block's `role="region"` root, which carries `tabindex="-1"` only until focus leaves it, because hiding the error box would otherwise drop focus to the document body (WCAG 2.4.3). No rule in `styles.css` styles that focus, so the browser's own indicator draws it rather than the `focus-ring` outline |
| `BLK-SECTION-PLAN` | Advance in your / learning plans | h3 (two lines) | `summary.mustache:83-87` + `styles.css:1776-1792` (print: `:1801-1807`) | `sectionheader_plan_line1` + `<br>` + `..._line2` | gated on `showsectionheaders` (`enable_section_headers`, default off) **and** on `rawDataset.hasplancards`; visibility is driven by the raw dataset, not the filtered one (`filters.js:936-944`), so a filter that yields zero cards keeps its header on screen. The base rule paints the text with a gradient through `background-clip: text` + `-webkit-text-fill-color: transparent`, which outranks any plain `color`; a plain `color` alone would paint nothing, which is why **print** — the one mode that still forces a literal colour — resets `background-image: none` and `-webkit-text-fill-color: currentcolor` first before setting `#000`. Dark needs no override any more: the gradient stops and the base `color` are all `--block-dimensions-*` tokens that flip on their own |
| `BLK-SECTION-COMP` | Find courses / for each competency | h3 (two lines) | `summary.mustache:99-103` | `sectionheader_competency_line1` / `..._line2` | same gate against `rawDataset.hascompetencies`; both headers share the single `.dims-section-header` rule (`styles.css:1776-1792`), so the gradient fill and its token-driven colours are common to them. Only **print** still needs a literal override — it drops the gradient and restores `-webkit-text-fill-color: currentcolor` before setting `color: #000` (`styles.css:1801-1807`); dark has no override left to cite |
| `BLK-FILTERS-HOST` | — | injection point | `summary.mustache:88` (plan) / `:104` (competency) | `data-filters-type` | one host per type, emptied and refilled wholesale by `renderFilterControls` (`filters.js:195-199, 284-295`); when no filter qualifies the host is set to `''` so no empty toolbar is left behind, and `FilterTabsNav.destroyAll` must run **before** the `innerHTML` swap or every `ResizeObserver` leaks |
| `BLK-GRID-PLAN` | Learning plans | `nav` > `ul[role=list]` | `summary.mustache:95-97` + `styles.css:492-518` | `data-cards-type="plan"` | each `<nav>` carries its own `aria-label` (`landmark_plans`) rather than `aria-labelledby` on the h3, so the landmark still has a name when section headers are off; `<li>`s are appended by `renderCardListIncremental` and hold the plan cards of the status bucket on screen. Laid out on `repeat(auto-fill, minmax(var(--dims-card-track-min), 1fr))` tracks since 2026-09-22: as a flex row the last card of an odd row grew to the full width, the same card in two sizes on one screen. The floor is `min(19rem, 100%)`, or `min(26rem, 100%)` when the list holds horizontal cards: the width sets the column count in a wide region, and the `100%` cap gives a block narrower than the floor one track as wide as the block instead of a track that overflows it |
| `FLT-STATUS` | Active / In review / Completed | pill radiogroup | `filters.js:89-127` (`renderStatusPills`) + `styles.css:1431-1471` | `data-status-filter`, `plancounts`, `hasactiveplans` | the first group of the plan filter bar, before the favourites pills, so on a phone it appears with the rest when the panel is opened. A bucket's pill is drawn when the bucket holds plan cards, when it is the bucket on screen, or — Active only — whenever the learner holds an active plan (`hasactiveplans`), and the group only when at least two pills qualify (`filters.js:90-100`). The count is the bucket's plan cards, `count_plans_by_bucket()` (`dataset_provider.php:175-204`), which leaves out a competencies-mode template, since that renders competency cards rather than a plan card; a pill whose count is 0 shows no number (`filters.js:112-114`). A click goes to `pickStatus` (`filters.js:1044-1129`): a bucket fetched before is swapped back in with no request, a new one is fetched by name and kept, and a failed fetch returns the grid to the bucket it left, with the favourites and tag filters it had when the switch began (`filters.js:1052-1056`, `:1118-1123`), and shows `BLK-ERROR`. A response that lands after a Retry has started a new session is dropped (`filters.js:1089-1091`, `:1111-1113`), and while a bucket is on its way the group ignores clicks and arrow keys (`FLT-STATUS-BUSY`) |
| `FLT-STATUS-BUSY` | — | radio state of `FLT-STATUS` | `filters.js:107-119` + `styles.css:758-761` | `aria-busy`, `.dims-status-spinner`, `state.statusLoading` | the pill of a bucket being fetched for the first time swaps its count for a spinner and carries `aria-busy="true"`. While it is busy a click on any other status pill is ignored (`filters.js:1045`) and the arrow keys do nothing in the group (`filter_tabs_nav.js:482-487`), so focus never lands on a radio that is not checked |
| `FLT-STATUS-EMPTY` | — | radio omitted from `FLT-STATUS` | `filters.js:94-96` | `plancounts` | a bucket with no plan cards, which is not on screen, draws no pill: an empty *In review* would read as "you have none", while on most sites it means "you cannot see them" — the review statuses are core's draft statuses and need `moodle/competency:planviewowndraft`, which no archetype holds. The Active pill is the exception, drawn without a number whenever `hasactiveplans` is true |
| `BLK-SKELETON` | — | placeholder cards | `filters.js:965-986` (`renderStatusSkeleton`) + `styles.css:708-756` | `.dims-skeleton-card` | drawn while a status bucket loads for the first time, as many cards as that bucket's pill counts, one to three; `aria-hidden`, with the grid marked `aria-busy` and the loading line beside it carrying the announcement. The pulse runs only under `prefers-reduced-motion: no-preference` (`styles.css:745-750`) |
| `BLK-GRID-COMP` | Competencies | `nav` > `ul[role=list]` | `summary.mustache:105-107` + `styles.css:520-524` | `data-cards-type="competency"`, `landmark_competencies` | the competency grid overrides only the shared grid's track floor, to `min(360px, 100%)`: 360px sets the column count in a wide region, and in a block narrower than that (Boost's 315px drawer, a phone) the single track takes the block's width instead of overflowing it, as the old hard `minmax(360px, 1fr)` did. Its cards are built from the active plans alone and stay as they are when the status bucket changes (`pickStatus`, `filters.js:1034-1035`) |
| `BLK-EMPTY` | — | muted status paragraph | `summary.mustache:109` + `filters.js:522-553` | `role=status`, `aria-live=polite` | single element reused for the three empty messages; recomputed on every render and hidden first, so a stale message can never survive a filter change, and left hidden until the session's first dataset has arrived (`state.datasetReady`) and while a status bucket loads (`filters.js:530-534`): before the first response the lists are empty because nothing has been fetched yet, so a render in that window (typing in the search, or the start of a Retry) says nothing rather than "no active plans" |
| `BLK-EMPTY-NOPLANS` | No active plans at the moment. | text state of `BLK-EMPTY` | `filters.js:536-540` | `noactiveplans` | only once the session's first dataset has arrived, while the active bucket is on screen and `hasactiveplans` is false. The block does not render at all for a user with no plan in any bucket (`block_dimensions.php:86-88`, through `summary::has_content():132-141`), and it opens on the first bucket holding a plan (`dataset_provider::opening_bucket():129-137`), so this is the AJAX-side mirror of a server-side gate, reachable only when the plan list changes between the page load and the web-service call, or when the gate failed open |
| `BLK-EMPTY-NOCOMP` | No competencies in your plans. | text state of `BLK-EMPTY` | `filters.js:542-552` | `nocompetencies` | only when **both** filtered lists are empty, no ghost card is visible — a visible ghost card means more items exist behind the favourites filter — **and** neither loaded list holds a card at all |
| `BLK-EMPTY-NORESULTS` | No results found. | text state of `BLK-EMPTY` | `filters.js:548-550` | `resultsnonefound` | both filtered lists are empty but the loaded dataset holds cards: the search or a filter hides every one of them, so the message says nothing matched rather than that nothing exists. It is the string the polite live region announces for zero results too |
| `BLK-LIVE-RESULTS` | {n} learning plans and {n} competencies found. | `visually-hidden` live region | `summary.mustache:111` + `filters.js:675-714` | `resultsfound` / `resultsnonefound`, `data-results-status` | `aria-live=polite`, debounced 600 ms so typing does not spam AT, with one pending timer per block, kept in a `WeakMap` keyed by the block's container, so two blocks on one page never cancel each other's announcement; silent until the session's first dataset has arrived and while a status bucket loads; blanked then rewritten on the next frame so two identical counts still fire |
| `BLK-LIVE-FAV` | Could not update favourites. Please try again. | `visually-hidden` live region | `summary.mustache:112` + `filters.js:716-735` | `favouriteerror`, `data-fav-status` | `aria-live=assertive` — used **only** for a failed `toggle_favourite` (`filters.js:893-896`); auto-clears after 5 s so the message does not linger in the AT cursor |
| `BLK-NOSCRIPT` | JavaScript is required… | `noscript` paragraph | `summary.mustache:114-116` + `styles.css:2131-2146` | `javascriptrequired` | the entire block is client-rendered, so without JS this is the *only* thing the user sees — hence the deliberate 1 rem size and ≥ 4.5:1 contrast (WCAG 1.4.3) |
| `FLT-BAR` | Filter learning plans / Filter competencies | `role=toolbar` | `filters.js:210` + `:288` + `styles.css:1263-1269` | `filterbyplan` / `filterbycompetency`, falling back to `filterall` | built per type, wrapped in `.dims-filters-panel-inner` and injected only when at least one group qualified (`hasAnyFilter`); flex-wrap means the pill groups reflow rather than scroll as a whole |
| `FLT-FAV-PILL` | My Favourites (N) | `role=radio` pill | `filters.js:158-166` + `styles.css:1936-1966` | `myfavourites`, `state.favouriteCountPlan` / `...Competency`, `data-fav-filter-type` | rendered only when `favouritesEnabled` **and** that type's favourite count is > 0, and for plans only in the active bucket, whose cards alone carry a star (`filters.js:142-150`); toggling it off while the full dataset is not loaded triggers a phase-2 fetch for that group alone (`filters.js:1423-1436`) |
| `FLT-ALL-PILL` | Show all (N) | `role=radio` pill | `filters.js:168-174` | `showallitems`, `state.totalplans` / `totalcompetencies`, `data-all-filter-type` | the mutually-exclusive twin of `FLT-FAV-PILL` (same radiogroup); clicking it calls `showAllItems`, which clears the favourite filter for that type and loads the missing group only when `hasnonfavourite*` says something is actually missing (`filters.js:1235-1261`) |
| `FLT-COUNT` | (N) | count badge | `filters.js:165` / `:173` (favourites, show all) / `:113` (status) + `styles.css:1995-2004` (rest: `:1982-1993`) | per-type counts from `state.js` `updateFavouriteCounts:202-205`; `plancounts` for the status pills | after a star toggle the counts are patched **in place** (`updateFavouritePillCounts`, `filters.js:652-673`); the bar is only rebuilt when a count crosses the 0 boundary and the pills appear/disappear (`filters.js:860-870`), otherwise focus would jump off the star the user just pressed (WCAG 2.4.3). The resting badge is `--block-dimensions-surface` under `--block-dimensions-ink-muted`, which flips on its own in dark — no separate dark rule to keep in sync any more; the *active* badge, inside any checked pill — favourites, show all or status — is `--block-dimensions-brand-ink` on `--block-dimensions-brand-tint` in both skins and needed no split either. That rule is keyed on the checked tab rather than on a pill type, because a checked status or show-all pill sits on the indicator, which paints `surface`, where the resting fill would give the badge no shape; `card_layout_test` pins it for every pill class `filters.js` draws |
| `FLT-TABS` | Filter … — {custom field name} | `role=radiogroup` platter | `filters.js:247-248` (tag) / `:155-156` (fav) / `:102-104` (status) + `styles.css:1316-1360` | `tag1label` / `tag2label` from `dataset_provider::get_customfield_name():741-759` | one group per enabled tag whose cards actually yield values (`getUniqueTagValues`, `filters.js:52-60`) — an enabled filter with no data renders nothing; the accessible name combines the type label with the admin's custom-field name so two groups never share a name (WCAG 2.4.6) |
| `FLT-TAB-ALL` | All | reset `role=radio` | `filters.js:249-252` | `filterall`, `data-filter-value=""` | always first in a tag group; carries `aria-checked=true` and the group's single `tabindex="0"` whenever that tag has no selection (roving tabindex) |
| `FLT-TAB` | {tag value} | `role=radio` pill | `filters.js:253-260` + `styles.css:1431-1471` | unique `card[tag]` values, sorted with `localeCompare` | selection is stored in `state.activeFilters[{type}_tag{n}]` and re-read by `syncFilterActiveState` (`filters.js:298-336`); tag matching is strict equality across groups (AND) — see `state.js` `isCardVisible:142-161`. Switching the status bucket clears both plan tag selections (`filters.js:1061-1062`); a switch whose fetch fails puts them back (`filters.js:1118-1123`) |
| `FLT-INDICATOR` | — | animated pill background | `filter_tabs_nav.js:99-101` (created) / `:378-391` (positioned) + `styles.css:1473-1489` | `--dims-tabs-indicator-start` / `--dims-tabs-indicator-width` | purely presentational (`pointer-events: none`); positions are written as CSS custom properties so the 320 ms transition does the animating, and `.dims-filter-tabs-no-transition` suppresses it during init and resize |
| `FLT-MASK` | — | scroll + fade layer | `filter_tabs_nav.js:94-97` + `styles.css:1380-1415` | `--dims-tabs-mask-color-left/right` | the actual scroll container; each edge fades **unless** the matching paddle is hidden (`styles.css:1362-1370`), so the fade always signals "there is more this way" |
| `FLT-PADDLE-L` | Scroll filters left | chevron button | `filter_tabs_nav.js:110-116` + `:430-434` + `styles.css:1502-1536` | `paddleleft` | mouse-only affordance: permanently `tabIndex=-1` because keyboard users move with arrow keys; hidden **and** `aria-hidden=true` + `disabled` at the left edge, un-hidden with `aria-hidden=false` otherwise — the two must stay in sync (`_updatePaddles:355-372`) |
| `FLT-PADDLE-R` | Scroll filters right | chevron button | `filter_tabs_nav.js:118-124` + `:436-440` + `styles.css:1538-1540` | `paddleright` | same contract at the right edge; both paddles are force-hidden and the mask marked `-noscroll` when the items fit (`props.scrollable`, `filter_tabs_nav.js:300`) |
| `FLT-SELECT` | Filter … — {custom field name} | native `<select>` | `filters.js:262-272` + `styles.css:1563-1600` | same values as `FLT-TAB`, `data-filter-field` | the alternative rendering when that tag's `*_displaymode` is `dropdown` (anything other than the literal `dropdown` falls back to `tabs`, `filters.js:236`); a `change` delegate writes straight into `state.activeFilters` (`filters.js:1459-1467`) |
| `FLT-CLEAR` | Clear filters | pill button | `filters.js:275-282` + `styles.css:1274-1309` | `clearfilters`, `fa fa-eraser`, `data-clear-type` | rendered once per bar but ships `display:none`; shown only while `hasActiveFiltersForType` is true (`state.js:184-195`, applied in `filters.js:341-346`). Clicking clears both tags **and** the favourite filter for that type only, and triggers a phase-2 load if that group was never fully fetched — for plans, only while the active bucket is on screen (`filters.js:1374-1394`) |
| `GST-CARD` | View more items (N) | dashed card button | `filters.js:484-502` (`appendGhostCardTo`) + `styles.css:2009-2044` | `ghostcardtitle` + count via `labelWithCount:65-67` | one per type, appended **into** the card list so the grid keeps its rhythm; drawn only when favourites are enabled, that type's favourite filter is active, and the remaining count is > 0 — the count is `total − loaded` while still in phase 1 and `total − favourites` once the full dataset is in (`renderGhostCards:441-482`). Clicking it is identical to `FLT-ALL-PILL` for that type (`filters.js:1405-1412`) |
| `GST-ICON` | — | dashed circle + plus | `filters.js:491-493` + `styles.css:2046-2067` | `fa fa-plus`, `aria-hidden` | 50 px circle; decorative — the button's accessible name is the `aria-label` carrying the counted title, so the icon and title text are never announced twice |
| `GST-TITLE` | View more items (N) | bold label | `filters.js:494-496` + `styles.css:2069-2080` | `ghostcardtitle` | the count is baked into the string at render time, so a favourite toggle that changes the remaining count requires the ghost cards to be re-created (they are: `renderGhostCards` removes every `.dims-ghost-card` first) |
| `GST-SUBTITLE` | Explore beyond favourites. | muted caption | `filters.js:497-499` + `styles.css:2082-2088` | `ghostcardsubtitle` | optional — falls back to an empty string if the label is missing, and the element is still emitted so the card's vertical rhythm does not shift |
| `RSP-STICKY` | — | sticky translucent header | `styles.css:288-311` | — | below 576 px `.dims-block-header` stacks, centres and sticks at `top: 60px` with a `blur(8px)` backdrop, so search and the filter toggle stay reachable while the card list scrolls. The `background` is `--block-dimensions-scrim`, which flips on its own — the separate dark override this used to need is gone |
| `RSP-SEARCH-STACK` | — | search row, stacked | `styles.css:307-310` | resets `max-width: 200px` → `100%` and `margin-left: auto` → `0` | on desktop the row is a 200 px island pushed to the right of the heading; below 576 px the header is already `flex-direction: column`, so the row must give up both constraints or it stays a narrow strip centred under the title |
| `RSP-TOGGLE` | Filters | mobile-only control | `styles.css:1605-1607` (mobile: `:1609-1637`) + `filters.js:1481-1497` | `aria-expanded` | 36 px circle, `display:none` on desktop; `[aria-expanded="true"]` shares the hover styling so the open state is visible without JS-set classes. All of its styling is nested in the same `max-width: 575.98px` query — a rule outside it would never match a control that only exists on mobile — and every declaration is a `--block-dimensions-*` token, so there is no separate dark skin left to repeat: rest, hover/`[aria-expanded="true"]` and the focus ring each flip on their own |
| `RSP-FILTERS-COLLAPSE` | — | collapsible filter panel | `styles.css:1642-1681` + `filters.js:1484-1487` | `.dims-filters-open` on the container | animated with `grid-template-rows: 0fr → 1fr` over `.dims-filters-panel-inner` (`overflow:hidden; min-height:0`) rather than `max-height`, so the transition works at any content height; pills also shrink to `0.8rem`, the bar centres its groups with a row gap of its own, and selects go full-width |
| `RSP-PILL-SMALL` | — | filter pill, compact | `styles.css:1662-1665` | `padding: .5rem 1rem` → `.375rem .625rem`, `font-size: .875rem` → `.8rem` | the only thing the mobile query changes about the pills themselves. The groups keep the desktop `flex-wrap` and simply reflow — centred, `.625rem` between rows and `.5rem` between groups (`styles.css:1670-1676`) — so a wide platter never becomes a horizontal scroller on a phone; the platter's own 44 px height is unchanged, which is why the pills lose padding rather than the shell losing height |
| `RSP-IMAGE-140` | — | card image band | `styles.css:447-451` (≥992 px) + `:453-457` (≤576 px) | `.competency-card-image` height `100px` / `120px` / `140px` | three fixed heights, not an aspect ratio: taller on a phone where the card is full-bleed, shorter on a wide desktop where the block column is narrow and a tall band would push the title below the fold. The plan card's own image stays at `120px` (`:561-568`); only the horizontal variant re-sizes, to a `160px` band when stacked (`:1097-1100`) — the migration also dropped the `@container` duplicate of this rule (stylelint rejects `@container`; see the fleet-wide note), so there is only the one `@media` version to cite now |
| `RSP-GRID` | — | container-driven card layout | `styles.css` CARD GRID section (`styles.css:492-524`) | `display: grid; grid-template-columns: repeat(auto-fill, minmax(var(--dims-card-track-min), 1fr)); gap: 1rem` on the list, the floor `min(19rem, 100%)` (plans), `min(26rem, 100%)` (horizontal plan cards) or `min(360px, 100%)` (competencies); the Bootstrap column widths neutralised on each `<li>` | deliberately replaces the Bootstrap `col-*` classes the `<li>`s still carry: a Moodle block can be narrow inside a wide viewport, so columns must respond to the **container**, not the viewport. `auto-fill` keeps the empty tracks, so a lone last card keeps its column width, and the `100%` cap keeps a single track inside a block narrower than the floor. The floor rides a custom property because Moodle's stylelint rejects `min()` inside `minmax()` and does not validate a declaration that reads `var()`; `card_layout_test` pins both the cap and the auto-fill tracks. **Re-based 2026-09-22 and 2026-09-23:** the flex row of 2026-09-05 stretched a lone last card and gave way to this grid, whose floors were bare lengths until the `100%` cap was added. **Re-based 2026-09-05** (to that flex row): before it, this was `grid-template-columns: repeat(auto-fill, minmax(clamp(240px, 30%, 500px), 1fr))` with `container-type: inline-size` on each `<li>`. A flex basis is still a percentage *of the container*, so 30% caps the row at three cards and keeps the container-relative behaviour; `clamp()` inside a length-valued property and `container-type` are both rejected by Moodle's stylelint. The competency grid stayed a real grid at `minmax(360px, 1fr)`, which is valid |
| `RSP-H-STACK` | — | horizontal plan card stacking | `styles.css` plan-card-horizontal responsive block | `@media (max-width: 575.98px)` | the horizontal plan card flips to a column and its image drops to a 160 px band. **Re-based 2026-09-05:** this was declared twice, and the second declaration — `@container dims-card-cell (max-width: 360px)`, the one that covered a narrow block on a wide screen — is gone with the container-query layer. Only the narrow-phone case is served now |
| — | — | two-phase load decision tree | `filters.js:1568-1689` | `favouritesonly`, `loadgroup`, `hasnonfavourite*`, `totalplans` / `totalcompetencies` | phase 1 asks for favourites only whenever favourites are enabled, and names no status bucket, so the server builds the one the block opens on. A search term already in the input (a Retry, or a value the browser put back) comes first: the favourites filter stays off and whatever the first response left out of the active plan bucket and the competency cards is fetched (`filters.js:1633-1641`). Otherwise, per group (`filters.js:1642-1670`): both groups have items but zero favourites → load everything (`loadgroup: ''`); only one group is favourite-less → load exactly that group and pin the other in phase-1 mode; no favourites at all → load everything; favourites disabled, nothing hidden, or everything already favourited → the first response *is* the full dataset and both `fullDatasetLoaded` flags are set true. Every phase-2 fetch names `planstatus: 'active'` (`filters.js:1169-1176`): competency cards come from the active plans alone, and every other bucket arrives whole. Get a flag wrong and the block either double-fetches or silently hides cards that were never requested |
| — | — | render-token guard | `filters.js:374-417` + `:920-921` | `container.dataset.renderToken` | `rerender` stamps a fresh token before each pass; `renderCardListIncremental` checks it before every 24-card batch and again after each batch resolves, resolving early instead of appending when a newer render has started — without it, a fast filter change interleaves stale cards into the fresh list and `cardsRendered` gets marked true for a list that was never finished. A response from a superseded load session is dropped the same way (`state.session`, `filters.js:1582-1591`) |

**Settings affecting this screen:** `show_heading` (the "My competencies" h2; unset counts as on),
`hide_block_title` (blanks the *block frame's* own title in `block_dimensions::specialization():59-63`,
independent of `show_heading`), `enable_search` (the whole search row **and** the mobile filter
toggle that lives in it — default off), `enable_section_headers` (both two-line h3 group titles —
default off), `enable_favourites` (both pills, the counts, the ghost card, the favourites-first
load and the stars themselves; always read through `dataset_provider::is_favourites_enabled()`, never
the raw config, so a fresh site does not disagree with the web service. With the setting off every
card arrives with `showfavourite` false, so neither card template draws a star
(`dataset_provider::favourite_fields()`, `dataset_provider.php:445-467`); the click delegate also
ignores a star while favourites are off, `filters.js:1397-1403`),
`enable_plan_tag1_filter` / `enable_plan_tag2_filter` / `enable_competency_tag1_filter` /
`enable_competency_tag2_filter` (each switches one filter group on, and it still renders nothing if
the loaded cards carry no values for that tag), and `plan_tag1_displaymode` / `plan_tag2_displaymode`
/ `competency_tag1_displaymode` / `competency_tag2_displaymode` (`tabs` — the scrolling pill platter
— or `dropdown`, the native select; anything else falls back to `tabs`). `plancard_layout` and
`enable_trail_links` reach only the card surfaces and are documented with those maps.
