# Field map — `BLK` / `FLT` / `GST` / `RSP` · Block shell, filter bar, ghost card, responsive

The block shell is the only server-rendered part: `block_dimensions::get_content()` builds the
`summary` renderable, `summary::export_for_template()` exports **labels and flags only**
(`labelsjson`, `filtersettingsjson`, `showheading` / `showsearch` / `showsectionheaders`,
`favouritesenabled`), and `summary.mustache` emits an empty skeleton. Everything inside it is
rendered client-side by `amd/src/filters.js` from **one** call to
`block_dimensions_get_block_dataset`; `amd/src/state.js` holds the pure filter/search state and
`amd/src/filter_tabs_nav.js` turns each pill group into a scrollable platter. The filter bar is
**not** a template at runtime — it is string-concatenated in `renderFilterControls()`
(`filters.js:67-198`); `templates/filters.mustache` is a documentation-only mirror whose edits have
no effect. Visual replica: the `screens/` files that coin the `BLK-*` (shell + states), `FLT-*`
(filter bar), `GST-*` (ghost card) and `RSP-*` (responsive) badges.

| ID | Label | Type | Origin | Data | Rule |
|---|---|---|---|---|---|
| `BLK-HEADING` | My competencies | h2 | `summary.mustache:39-43` + `styles.css:25-30` | `{{#str}}mycompetencies{{/str}}`, id `block-dimensions-heading-{containerid}` | gated on `showheading` (`dataset_provider::get_ui_config():492-497` — "never set" counts as **on**, unlike every other flag); the outer region's `aria-label` uses the same string, so hiding the heading never removes the landmark name |
| `BLK-SEARCH` | Search cards / Search… | text input | `summary.mustache:44-59` + `styles.css:63-84` | `searchlabel` (visually-hidden `<label for>`), `search_placeholder` | whole `.dims-search-row` gated on `showsearch` (`enable_search`, default **off**); typing debounces 120 ms (`filters.js:856-898`), normalises accents (`state.js:33-36`), and **force-clears** the favourite filter and all four tag filters so a search always spans the full dataset |
| `BLK-SEARCH-ICON` | — | magnifier icon | `summary.mustache:50` + `styles.css:54-61` | `fa fa-search`, `aria-hidden` | decorative only; absolutely positioned inside the wrapper with `pointer-events: none` so it never eats a click meant for the input |
| `BLK-SEARCH-CLEAR` | Clear (`core`) | icon button | `summary.mustache:56-58` + `styles.css:86-110` | `fa fa-times` | ships `style="display:none"`; switched to `flex` the moment the input is non-empty (`filters.js:858-860`) and back to `none` on click, which also returns focus to the input (`filters.js:900-911`) |
| `BLK-FILTER-TOGGLE` | Filters | icon button | `summary.mustache:60-65` + `styles.css:1385-1442` | `togglefilters`, `fa fa-sliders`, `aria-expanded` | `display:none` above 576 px — it exists only to toggle `.dims-filters-open`; a `matchMedia` listener resets both the class and `aria-expanded` when the viewport grows back (`filters.js:1047-1062`), otherwise a panel opened on mobile stays "expanded" to AT on desktop. Because the control exists **only** below the breakpoint, its dark rules have to live inside that same media query (`styles.css:1422-1441`): rest is `#212529` fill / `#495057` border / `#adb5bd` icon at 7.43:1, hover and `[aria-expanded="true"]` swap border and icon to `#6ea8fe` (6.39:1), and the focus ring flips to the same accent. Until those rules landed the button had no dark override, so a light `#f8f9fa` disc sat on the dark sticky header |
| `BLK-LOADING` | Loading (`core`) | status text | `summary.mustache:70-72` | `role=status`, `aria-live=polite` | visible on first paint and re-shown for every phase-2 fetch (`showLoading`, `filters.js:443-448`); it is the only content until the web service answers |
| `BLK-ERROR` | — | `alert alert-warning` | `summary.mustache:74-79` | `loaderror` | any rejected fetch or failed render; `showError` (`filters.js:457-477`) deliberately blanks the text, un-hides the box, then writes the message on the **next frame** — writing into a `display:none` `role="alert"` is not announced by several screen readers (WCAG 4.1.3) |
| `BLK-ERROR-RETRY` | Retry | button | `summary.mustache:76-78` + `styles.css:2061-2064` | `retry` | re-runs `loadData` from scratch, i.e. it restarts at phase 1, not at the failed phase (`filters.js:1039-1044`) |
| `BLK-SECTION-PLAN` | Advance in your / learning plans | h3 (two lines) | `summary.mustache:81-85` + `styles.css:1596-1607` (dark: `:1628-1633`, print: `:1635-1641`) | `sectionheader_plan_line1` + `<br>` + `..._line2` | gated on `showsectionheaders` (`enable_section_headers`, default off) **and** on `rawDataset.hasplancards`; visibility is driven by the raw dataset, not the filtered one (`filters.js:740-746`), so a filter that yields zero cards keeps its header on screen. The base rule paints the text with a gradient through `background-clip: text` + `-webkit-text-fill-color: transparent`, which outranks any plain `color`; the dark and print overrides used to set `color` alone and therefore painted nothing at all, so both now reset `background-image: none` and `-webkit-text-fill-color: currentcolor` first and their `#dee2e6` / `#000` actually applies |
| `BLK-SECTION-COMP` | Find courses / for each competency | h3 (two lines) | `summary.mustache:97-101` | `sectionheader_competency_line1` / `..._line2` | same gate against `rawDataset.hascompetencies`; both headers share the single `.dims-section-header` rule, so the gradient fill and its two escapes are common to them — the dark (`#dee2e6`) and print (`#000`) overrides drop the gradient and restore `-webkit-text-fill-color: currentcolor` before setting `color`, which is what makes them visible at all (`styles.css:1617-1630`) |
| `BLK-FILTERS-HOST` | — | injection point | `summary.mustache:86` (plan) / `:102` (competency) | `data-filters-type` | one host per type, emptied and refilled wholesale by `renderFilterControls` (`filters.js:67-72, 187-197`); when no filter qualifies the host is set to `''` so no empty toolbar is left behind, and `FilterTabsNav.destroyAll` must run **before** the `innerHTML` swap or every `ResizeObserver` leaks |
| `BLK-GRID-PLAN` | Learning plans | `nav` > `ul[role=list]` | `summary.mustache:93-95` + `styles.css:385-405` | `data-cards-type="plan"` | each `<nav>` carries its own `aria-label` (`landmark_plans`) rather than `aria-labelledby` on the h3, so the landmark still has a name when section headers are off; `<li>`s are appended by `renderCardListIncremental` |
| `BLK-GRID-COMP` | Competencies | `nav` > `ul[role=list]` | `summary.mustache:103-105` + `styles.css:1642-1653` | `data-cards-type="competency"`, `landmark_competencies` | competency grid overrides the shared auto-fill with a hard `minmax(360px, 1fr)` — competency cards do not compress below that |
| `BLK-EMPTY` | — | muted status paragraph | `summary.mustache:107` + `filters.js:418-441` | `role=status`, `aria-live=polite` | single element reused for both empty messages; recomputed on every render and hidden first, so a stale message can never survive a filter change |
| `BLK-EMPTY-NOPLANS` | No active plans at the moment. | text state of `BLK-EMPTY` | `filters.js:426-430` | `noactiveplans` | wins over everything else when `hasactiveplans` is false; note the block also refuses to render at all in that case (`summary::has_content():104-107`), so this is the AJAX-side mirror of a server-side gate |
| `BLK-EMPTY-NOCOMP` | No competencies in your plans. | text state of `BLK-EMPTY` | `filters.js:432-440` | `nocompetencies` | only when **both** filtered lists are empty **and** no ghost card is visible — a visible ghost card means more items exist behind the favourites filter, so "no competencies" would be a lie |
| `BLK-LIVE-RESULTS` | {n} learning plans and {n} competencies found. | `sr-only` live region | `summary.mustache:109` + `filters.js:514-537` | `resultsfound` / `resultsnonefound`, `data-results-status` | `aria-live=polite`, debounced 600 ms so typing does not spam AT; blanked then rewritten on the next frame so two identical counts still fire |
| `BLK-LIVE-FAV` | Could not update favourites. Please try again. | `sr-only` live region | `summary.mustache:110` + `filters.js:544-558` | `favouriteerror`, `data-fav-status` | `aria-live=assertive` — used **only** for a failed `toggle_favourite`; auto-clears after 5 s so the message does not linger in the AT cursor |
| `BLK-NOSCRIPT` | JavaScript is required… | `noscript` paragraph | `summary.mustache:112-114` + `styles.css:2015-2024` | `javascriptrequired` | the entire block is client-rendered, so without JS this is the *only* thing the user sees — hence the deliberate 1 rem size and ≥ 4.5:1 contrast (WCAG 1.4.3) |
| `FLT-BAR` | Filter learning plans / Filter competencies | `role=toolbar` | `filters.js:84` + `:186` (mirror: `filters.mustache:48`) + `styles.css:1068-1074` | `filterbyplan` / `filterbycompetency`, falling back to `filterall` | built per type, wrapped in `.dims-filters-panel-inner` and injected only when at least one group qualified (`hasAnyFilter`); flex-wrap means the pill groups reflow rather than scroll as a whole |
| `FLT-FAV-PILL` | My Favourites (N) | `role=radio` pill | `filters.js:103-110` + `styles.css:1774-1796` | `myfavourites`, `state.favouriteCountPlan` / `...Competency`, `data-fav-filter-type` | rendered only when `favouritesEnabled` **and** that type's favourite count is > 0; toggling it off while the full dataset is not loaded triggers a phase-2 fetch for that group alone (`filters.js:995-1017`) |
| `FLT-ALL-PILL` | Show all (N) | `role=radio` pill | `filters.js:113-118` | `showallitems`, `state.totalplans` / `totalcompetencies`, `data-all-filter-type` | the mutually-exclusive twin of `FLT-FAV-PILL` (same radiogroup); clicking it calls `showAllItems`, which clears the favourite filter for that type and loads the missing group only when `hasnonfavourite*` says something is actually missing (`filters.js:823-849`) |
| `FLT-COUNT` | (N) | count badge | `filters.js:109` / `:117` + `styles.css:1836-1855` (dark: `:1536-1540`) | per-type counts from `state.js` `updateFavouriteCounts:160-163` | after a star toggle the counts are patched **in place** (`updateFavouritePillCounts`, `filters.js:484-505`); the bar is only rebuilt when a count crosses the 0 boundary and the pills appear/disappear, otherwise focus would jump off the star the user just pressed (WCAG 2.4.3). The resting badge now has its own dark rule — `#495057` fill with `#dee2e6` text, 6.28:1 — where before it had none and kept the light `#e9ecef` / `#495057` pair as a foreign light island on the `#212529` platter; the *active* badge inside a checked pill stays `#cfe2ff` / `#0f4d85` in both skins, which is 6.60:1 and needed no split |
| `FLT-TABS` | Filter … — {custom field name} | `role=radiogroup` platter | `filters.js:149-150` (tag) / `:99-100` (fav) + `styles.css:1108-1155` | `tag1label` / `tag2label` from `dataset_provider::get_customfield_name():537-557` | one group per enabled tag whose cards actually yield values (`getUniqueTagValues`, `filters.js:50-58`) — an enabled filter with no data renders nothing; the accessible name combines the type label with the admin's custom-field name so two groups never share a name (WCAG 2.4.6) |
| `FLT-TAB-ALL` | All | reset `role=radio` | `filters.js:151-154` | `filterall`, `data-filter-value=""` | always first in a tag group; carries `aria-checked=true` and the group's single `tabindex="0"` whenever that tag has no selection (roving tabindex) |
| `FLT-TAB` | {tag value} | `role=radio` pill | `filters.js:155-162` + `styles.css:1220-1259` | unique `card[tag]` values, sorted with `localeCompare` | selection is stored in `state.activeFilters[{type}_tag{n}]` and re-read by `syncFilterActiveState` (`filters.js:204-211`); tag matching is strict equality across groups (AND) — see `state.js` `isCardVisible:100-119` |
| `FLT-INDICATOR` | — | animated pill background | `filter_tabs_nav.js:100-102` (created) / `:379-392` (positioned) + `styles.css:1262-1277` | `--dims-tabs-indicator-start` / `--dims-tabs-indicator-width` | purely presentational (`pointer-events: none`); positions are written as CSS custom properties so the 320 ms transition does the animating, and `.dims-filter-tabs-no-transition` suppresses it during init and resize |
| `FLT-MASK` | — | scroll + fade layer | `filter_tabs_nav.js:96-98` + `styles.css:1185-1215` | `--dims-tabs-mask-color-left/right` | the actual scroll container; each edge fades **unless** the matching paddle is hidden (`styles.css:1169-1176`), so the fade always signals "there is more this way" |
| `FLT-PADDLE-L` | Scroll filters left | chevron button | `filter_tabs_nav.js:111-117` + `:431-435` + `styles.css:1290-1324` | `paddleleft` | mouse-only affordance: permanently `tabIndex=-1` because keyboard users move with arrow keys; hidden **and** `aria-hidden=true` + `disabled` at the left edge, un-hidden with `aria-hidden=false` otherwise — the two must stay in sync (`_updatePaddles:356-373`) |
| `FLT-PADDLE-R` | Scroll filters right | chevron button | `filter_tabs_nav.js:119-125` + `:437-441` + `styles.css:1326-1328` | `paddleright` | same contract at the right edge; both paddles are force-hidden and the mask marked `-noscroll` when the items fit (`props.scrollable`, `filter_tabs_nav.js:301`) |
| `FLT-SELECT` | Filter … — {custom field name} | native `<select>` | `filters.js:165-173` + `styles.css:1351-1380` | same values as `FLT-TAB`, `data-filter-field` | the alternative rendering when that tag's `*_displaymode` is `dropdown` (anything other than the literal `dropdown` falls back to `tabs`, `filters.js:138`); a `change` delegate writes straight into `state.activeFilters` (`filters.js:1029-1037`) |
| `FLT-CLEAR` | Clear filters | pill button | `filters.js:178-184` + `styles.css:1068-1100` | `clearfilters`, `fa fa-eraser`, `data-clear-type` | rendered once per bar but ships `display:none`; shown only while `hasActiveFiltersForType` is true (`state.js:142-153`, applied in `filters.js:241-246`). Clicking clears both tags **and** the favourite filter for that type only, and triggers a phase-2 load if that group was never fully fetched (`filters.js:947-965`) |
| `GST-CARD` | View more items (N) | dashed card button | `filters.js:380-398` (`appendGhostCardTo`) + `styles.css:1849-1881` | `ghostcardtitle` + count via `labelWithCount:63-65` | one per type, appended **into** the card list so the grid keeps its rhythm; drawn only when favourites are enabled, that type's favourite filter is active, and the remaining count is > 0 — the count is `total − loaded` while still in phase 1 and `total − favourites` once the full dataset is in (`renderGhostCards:337-378`). Clicking it is identical to `FLT-ALL-PILL` for that type (`filters.js:977-983`) |
| `GST-ICON` | — | dashed circle + plus | `filters.js:387-389` + `styles.css:1884-1900` | `fa fa-plus`, `aria-hidden` | 50 px circle; decorative — the button's accessible name is the `aria-label` carrying the counted title, so the icon and title text are never announced twice |
| `GST-TITLE` | View more items (N) | bold label | `filters.js:390-392` + `styles.css:1903-1913` | `ghostcardtitle` | the count is baked into the string at render time, so a favourite toggle that changes the remaining count requires the ghost cards to be re-created (they are: `renderGhostCards` removes every `.dims-ghost-card` first) |
| `GST-SUBTITLE` | Explore beyond favourites. | muted caption | `filters.js:393-395` + `styles.css:1916-1921` | `ghostcardsubtitle` | optional — falls back to an empty string if the label is missing, and the element is still emitted so the card's vertical rhythm does not shift |
| `RSP-STICKY` | — | sticky translucent header | `styles.css:117-140` (dark: `:143-149`) | — | below 576 px `.dims-block-header` stacks, centres and sticks at `top: 60px` with a `blur(8px)` backdrop, so search and the filter toggle stay reachable while the card list scrolls |
| `RSP-SEARCH-STACK` | — | search row, stacked | `styles.css:136-139` | resets `max-width: 200px` → `100%` and `margin-left: auto` → `0` | on desktop the row is a 200 px island pushed to the right of the heading; below 576 px the header is already `flex-direction: column`, so the row must give up both constraints or it stays a narrow strip centred under the title |
| `RSP-TOGGLE` | Filters | mobile-only control | `styles.css:1385-1442` (dark: `:1433-1452`) + `filters.js:1047-1062` | `aria-expanded` | 36 px circle, `display:none` on desktop; `[aria-expanded="true"]` shares the hover styling so the open state is visible without JS-set classes. The dark skin is nested in the same `max-width: 575.98px` query — a rule outside it would never match a control that only exists on mobile — and repeats the pairing: `#212529` disc with a `#495057` border and an `#adb5bd` icon (7.43:1) at rest, `#6ea8fe` border, icon and focus ring (6.39:1) once hovered or expanded |
| `RSP-FILTERS-COLLAPSE` | — | collapsible filter panel | `styles.css:1447-1475` + `filters.js:1049-1052` | `.dims-filters-open` on the container | animated with `grid-template-rows: 0fr → 1fr` over `.dims-filters-panel-inner` (`overflow:hidden; min-height:0`) rather than `max-height`, so the transition works at any content height; pills also shrink to `0.8rem` and selects go full-width |
| `RSP-PILL-SMALL` | — | filter pill, compact | `styles.css:1467-1470` | `padding: .5rem 1rem` → `.375rem .625rem`, `font-size: .875rem` → `.8rem` | the only thing the mobile query changes about the pills — the groups keep the desktop `flex-wrap` and simply reflow, so a wide platter never becomes a horizontal scroller on a phone; the platter's own 44 px height is unchanged, which is why the pills lose padding rather than the shell losing height |
| `RSP-IMAGE-140` | — | card image band | `styles.css:294-298` (≥992 px) + `:300-304` (≤576 px) | `.competency-card-image` height `100px` / `120px` / `140px` | three fixed heights, not an aspect ratio: taller on a phone where the card is full-bleed, shorter on a wide desktop where the block column is narrow and a tall band would push the title below the fold. The plan card's own image stays at `120px` (`:442-449`); only the horizontal variant re-sizes, to a `160px` band when stacked (`:771-774`, `:791-794`) |
| `RSP-GRID` | — | container-driven card grid | `styles.css:385-405` (competency override `:1653-1657`) | `grid-template-columns: repeat(auto-fill, minmax(clamp(240px, 30%, 500px), 1fr))` | deliberately replaces the Bootstrap `col-*` classes the `<li>`s still carry: a Moodle block can be narrow inside a wide viewport, so columns must respond to the **container**, not the viewport; each `<li>` becomes `container-type: inline-size` named `dims-card-cell` |
| `RSP-H-STACK` | — | horizontal plan card stacking | `styles.css:759-775` (viewport) + `:779-795` (container) | `@container dims-card-cell (max-width: 360px)` | the horizontal plan card flips to a column and its image drops to a 160 px band; the media query and the container query are duplicated on purpose — one covers a narrow phone, the other a narrow block on a wide screen |
| — | — | two-phase load decision tree | `filters.js:1080-1164` | `favouritesonly`, `loadgroup`, `hasnonfavourite*`, `totalplans` / `totalcompetencies` | phase 1 asks for favourites only whenever favourites are enabled. Then, per group: both groups have items but zero favourites → load everything (`loadgroup: ''`); only one group is favourite-less → load exactly that group and pin the other in phase-1 mode; no favourites at all → load everything; favourites disabled, nothing hidden, or everything already favourited → the first response *is* the full dataset and both `fullDatasetLoaded` flags are set true. Get a flag wrong and the block either double-fetches or silently hides cards that were never requested |
| — | — | render-token guard | `filters.js:274-317` + `:726-731` | `container.dataset.renderToken` | `rerender` stamps a fresh token before each pass; `renderCardListIncremental` checks it before every 24-card batch and again after each batch resolves, resolving early instead of appending when a newer render has started — without it, a fast filter change interleaves stale cards into the fresh list and `cardsRendered` gets marked true for a list that was never finished |

**Settings affecting this screen:** `show_heading` (the "My competencies" h2; unset counts as on),
`hide_block_title` (blanks the *block frame's* own title in `block_dimensions::specialization():57-61`,
independent of `show_heading`), `enable_search` (the whole search row **and** the mobile filter
toggle that lives in it — default off), `enable_section_headers` (both two-line h3 group titles —
default off), `enable_favourites` (star buttons, both pills, the counts and the ghost card;
always read through `dataset_provider::is_favourites_enabled()`, never the raw config, so a fresh
site does not disagree with the web service), `enable_plan_tag1_filter` /
`enable_plan_tag2_filter` / `enable_competency_tag1_filter` / `enable_competency_tag2_filter` (each
switches one filter group on, and it still renders nothing if the loaded cards carry no values for
that tag), and `plan_tag1_displaymode` / `plan_tag2_displaymode` /
`competency_tag1_displaymode` / `competency_tag2_displaymode` (`tabs` — the scrolling pill platter —
or `dropdown`, the native select; anything else falls back to `tabs`). `plancard_layout` and
`enable_trail_links` reach only the card surfaces and are documented with those maps.
